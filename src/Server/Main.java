package Server;

import Utils.FileManager;
import Utils.Mail;

public class Main {
    public static void main(String [ ] args)
    {
        //Server server = new Server(2001);

        //server.launch();
    }
}